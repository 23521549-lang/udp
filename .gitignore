node_modules/
dist/
build/
.turbo/
*.tsbuildinfo

# Env — chỉ commit file mẫu
.env
.env.*
!.env.example

# Prisma
packages/db/prisma/*.db
packages/db/prisma/*.db-journal

# Prisma 7 sinh client dưới dạng mã TypeScript vào chính project (2,4 MB, 31 file)
# thay vì vào node_modules như các bản trước. Đây là ARTIFACT chứ không phải mã
# nguồn: nó suy ra hoàn toàn từ schema.prisma, nên commit vào chỉ làm mọi diff nhiễu.
# Sau khi clone, và sau mỗi lần sửa schema, chạy: pnpm db:generate
packages/db/src/generated/

# ============================================================
# Bí mật — TUYỆT ĐỐI không commit.
# Đề tài xử lý credential cloud thật; một service-account.json
# lọt lên GitHub sẽ bị bot quét trong vài phút.
# ============================================================
*.pem
*.key
*.p12
*.pfx
kubeconfig*
credentials.json
service-account*.json
gcp-*.json
azure-*.json
.aws/
.kube/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Test / báo cáo
coverage/
playwright-report/
test-results/
*.log
