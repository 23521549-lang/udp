{
  "name": "@udp/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "//": "Ai nạp .env: prisma.config.ts cho đường CLI của Prisma, @udp/config/env cho seed và runtime, còn script quản trị trong scripts/ tự nạp .env gốc (dotenv, không ghi đè biến đã có) vì chúng không import @udp/config — entry đó validate toàn bộ env lúc nạp và sẽ sập trước khi script kịp báo biến nào thiếu",
    "//scratch": "test:scratch, verify-chain, scratch-drop, ci-bootstrap cùng dùng helpers/scratch-database.ts — một database dùng-một-lần trên chính Supabase (§13.5). test:scratch là đúng lệnh CI chạy; verify-chain là lượt nhanh cho người sửa migration",
    "//migrate": "migrate/studio dùng DATABASE_URL_DIRECT: chúng chạy nhiều statement nên không an toàn qua pooler transaction mode (ADR-05, §15.3)",
    "generate": "prisma generate",
    "validate": "prisma validate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:reset": "prisma migrate reset",
    "studio": "prisma studio",
    "format": "prisma format",
    "seed": "tsx prisma/seed.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:scratch": "tsx scripts/test-scratch.ts",
    "service-login": "tsx scripts/service-login.ts",
    "verify-chain": "tsx scripts/verify-chain.ts",
    "scratch-drop": "tsx scripts/scratch-drop.ts",
    "ci-bootstrap": "tsx scripts/ci-bootstrap.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.10.0",
    "@prisma/client": "^7.10.0",
    "@udp/config": "workspace:*",
    "@udp/shared-types": "workspace:*",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/pg": "^8.11.10",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "prisma": "^7.10.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
